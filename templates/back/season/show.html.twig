{% extends 'back/layout_back.html.twig' %}

{% block title %}Season{% endblock %}

{% block body %}
    <h1>Season</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ season.id }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ season.number }}</td>
            </tr>
            <tr>
                <th>EpisodesNumber</th>
                <td>{{ season.episodesNumber }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_movie_index') }}">back to list</a>

    <a href="{{ path('app_back_season_edit', {'id': season.movie.id, 'idSeason': season.id}) }}">edit</a>

    {{ include('back/season/_delete_form.html.twig') }}
{% endblock %}
